// import { updateCityTable } from '../src/main';

// // jest.mock('../src/main', () => ({
// //     ...jest.requireActual('../src/main'),
// //     getCitiesFromLocalStorage: jest.fn(),
// //     showCardByName: jest.fn(),
// // }));

// const { getCitiesFromLocalStorage, showCardByName } = jest.requireMock('../src/main');

// describe('updateCityTable', () => {

//     beforeEach(() => {
//         document.body.innerHTML = `
//             <div class="cityTable"></div>
//             <div class="cityTable"></div>
//         `;
//     });

//     afterEach(() => {
//         jest.clearAllMocks();
//     });

//     it('removes extra cityTables and updates the first cityTable based on local storage', async () => {
//         const cities = ['City1', 'City2'];
//         getCitiesFromLocalStorage.mockReturnValue(cities);

//         await updateCityTable();

//         const cityTables = document.querySelectorAll('.cityTable');
//         expect(cityTables.length).toBe(1);

        
//         const tableRows = cityTables[0].querySelectorAll('tr');
//         expect(tableRows).toHaveLength(cities.length);
//         expect(tableRows[0].textContent).toBe('City1');
//         expect(tableRows[1].textContent).toBe('City2');

//         // Simulating a click event on the cities
//         tableRows[0].click();
//         expect(showCardByName).toHaveBeenCalledWith('City1');
        
//         tableRows[1].click();
//         expect(showCardByName).toHaveBeenCalledWith('City2');
//     });
// });
